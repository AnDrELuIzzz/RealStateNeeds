package org.example.strategy;

import org.example.model.Property;
import java.util.List;

/**
 * Enum para definir a ordem de classificação.
 * ASCENDING: Ordem crescente (menor para maior)
 * DESCENDING: Ordem decrescente (maior para menor)
 */
public enum SortOrder {
    ASCENDING,
    DESCENDING
}